<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>DsCabinetWidgets</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

  <link href="/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/font-awesome.min.css" rel="stylesheet">
</head>
<body>
<div class="container">
  <%= yield %>
</div>

<script src="/js/bootstrap.js"></script>
<script type="text/javascript">
  var parent_url = decodeURIComponent(document.location.hash.replace(/^#/, ''));

  function send(msg) {
    XD.postMessage(msg, parent_url, parent);
  }

  function parent_call(options) {
    console.log('external call', options.domain)
  }

  $(function(){
    // send($('body').height());
    send($('body').height() + 100);
    // widgetMessage({domain: 'local-demo.sredda.ru:3000'});
  });

  function OpenPayWindow(url) {
    window.open(url);
    return false;
  }

</script>
</body>
</html>
